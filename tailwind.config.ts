import type { Config } from 'tailwindcss';

export default {
  corePlugins: {
    outline: false,
    ringColor: false,
    ringOffsetColor: false,
    ringOffsetWidth: false,
    ringWidth: false,
  },
} satisfies Config;
