// Route owned by Jim (AUTH-WAVE-1 gate target). Body re-exports Dwight's
// OpeningScreen per the stub's handoff note — welcome carousel + Log in /
// Sign up / guest browse (ref: prototypes/screens/opening.html).
export { OpeningScreen as default } from '../../src/auth/OpeningScreen';
